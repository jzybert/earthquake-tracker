<h1>Edit Tracked Area</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.tracked_earthquake_path(@conn, :update, Map.put(@tracked_earthquake, "user_id", @current_user.id))) %>

<span><%= link "Back", to: Routes.tracked_earthquake_path(@conn, :index), class: "btn btn-secondary" %></span>
